<html lang="en">
  <%- include('head', { title }) %>
  <link rel="stylesheet" href="messages.css" />
  <body>
    <%- include('nav', { title, links }) %>
    <ul class="messages" role="list">
      <% messages.forEach(message => { %>
      <%- include('message', { message, showMessageLinks }) %>
      <% }); %>
    </ul>
    <% if (showMessageLinks) { %>
    <a class="styled" href="/new">new message</a>
    <% } %>
  </body>
</html>
