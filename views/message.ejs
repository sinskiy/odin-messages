<li>
  <p><%= message.user %></p>
  <p><%= message.text %></p>
  <p>
    <time datetime="<%= message.added %>"
      ><%= message.added.toLocaleString() %></time
    >
  </p>
  <% if (showOpen) { %>
  <a href="/<%= message.id %>">open</a>
  <% } %>
</li>
