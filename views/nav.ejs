<header>
  <h1><%= title %></h1>
  <ul role="list">
    <% links.forEach(link => { %>
    <li>
      <a href="<%= link.url %>" class="styled"><%= link.name %></a>
    </li>
    <% }) %>
  </ul>
</header>
